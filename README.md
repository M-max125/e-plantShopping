# coding-project-template e-plantShopping

 - integrated Redux within thisr React application and applied Redux concepts like actions, reducers, and the store.
 - dynamically rendered data fetched from an array of objects onto the UI => mapped over arrays to generate lists of components.

 - handled user events such as button selection and triggered corresponding actions to add and remove items in the cart.
